server:
  port: 8080

spring:
  sql:
    init:
      mode: always
  datasource:
    url: jdbc:postgresql://localhost:5332/ecommerce
    username: postgres
    password: AxOmSaPe15092002
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show_sql: true
  main:
    web-application-type: servlet
